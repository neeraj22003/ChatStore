name: chat_shop
description: "A new Flutter project."
publish_to: 'none'
version: 1.2.0+2

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  http: ^1.6.0
  flutter_dotenv: ^6.0.0
  flutter_html: ^3.0.0
  shared_preferences: ^2.3.5
  sqflite: ^2.4.1
  path: ^1.9.1
  sqflite_common_ffi: ^2.4.0+2
  currency_sdk: ^1.0.1
  geolocator: ^14.0.2
  geocoding: ^5.0.0
  
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  cached_network_image: ^3.4.1
  html: ^0.15.6
  
  flutter_staggered_grid_view: ^0.7.0
  animated_text_kit: ^4.3.0
  skeletonizer: ^2.1.3
  shimmer: ^3.0.0
  flutter_bloc: ^9.1.1
  bloc: ^9.2.1
  sahih_validator: ^2.1.2
  google_sign_in: ^7.2.0
  get_it: ^9.2.1
  uuid: ^4.5.3
  firebase_auth_mocks: ^0.15.2
  fake_cloud_firestore: ^4.1.1
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
 
  test: ^1.30.0

flutter:
  uses-material-design: true
  assets:
    - ebuy_token.env
    - assets/

# This block must have ZERO indentation to be recognized
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/app.png"
  remove_alpha_ios: true